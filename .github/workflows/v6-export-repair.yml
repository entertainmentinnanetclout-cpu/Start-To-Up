name: V6 export syntax repair

on:
  push:
    branches:
      - feat/website-studio-v6-builder-suite
    paths:
      - scripts/v6-export-repair-trigger.txt

permissions:
  contents: write

jobs:
  repair:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/website-studio-v6-builder-suite
          fetch-depth: 1
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Patch V6 generated-source syntax
        run: node scripts/patch-v6-export.mjs
      - name: Commit repaired export source
        run: |
          git config user.name "start-to-up-ci"
          git config user.email "actions@users.noreply.github.com"
          git add src/lib/website-studio-project-export-v6.ts
          if git diff --cached --quiet; then
            echo "No repair required"
          else
            git commit -m "fix: normalize V6 generated source templates"
            git push origin HEAD:feat/website-studio-v6-builder-suite
          fi
