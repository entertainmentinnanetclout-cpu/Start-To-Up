Temporary recovery note. The custom TanStack Start server and start-instance wrappers were removed because they could convert framework control-flow responses into generic 500 HTML before the application rendered. The app now relies on TanStack Start's default server entry and default start instance while retaining the Vercel Nitro preset. Remove this note after production verification.